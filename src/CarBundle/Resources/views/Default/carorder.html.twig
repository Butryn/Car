{% extends 'CarBundle::layout.html.twig' %}
{% block content %}
<table>
	<tr>
		<td><img src="{{car.img}}"</td>
		<td>{{car.brand}}</td>
		<td>{{car.model}}</td>
		<td>{{car.power}}</td>
		<td>{{car.type}}</td>
		<td>{{car.price}}</td>
	</tr>
</table>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Wypożycz" />
    {{ form_end(form) }}
{% endblock %}